language: node_js

node_js:
  - 'node'
  - 'lts/*'

cache:
  directories:
    - 'node_modules'

script:
  - npm install -g now
  - npm install
  - npm run prettier:check

deploy:
  provider: script
  script: now
  on:
    branch: master
